import { Tree } from '@nx/devkit';
export interface FileContents {
    path: string;
    content?: string | string[];
    isBinary?: boolean;
    children?: Record<string, FileContents>;
}
export declare function getRecursiveFileContents(tree: Tree, path: string, includeBinaries?: boolean): Record<string, FileContents>;
export declare function formatOutput(value: string): string[];
