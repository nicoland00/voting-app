export declare function runCommand(cwd: string, commands: string | string[]): {
    executor: string;
    options: {
        cwd: string;
        commands: string[];
        parallel: boolean;
    };
};
